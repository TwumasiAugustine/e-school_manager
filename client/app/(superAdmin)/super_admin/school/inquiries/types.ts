// Define shared types for the School Inquiry module
import { SchoolInquiryData } from '@/types/school';

// Export the type with a different name to avoid confusion
export type SchoolInquiry = SchoolInquiryData;
